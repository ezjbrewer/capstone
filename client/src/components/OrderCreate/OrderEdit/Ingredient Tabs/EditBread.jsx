// set breadChoice with bread ingredient from sent sandwichIngredients
    // have that bread ingredient be checked by default when rendering options

// if user selects a bread, set BreadChoice with that breadChoice