// Declare empty object var, breadChoice

// Render a list of bread ingredients using radio inputs
    // If the bread ingredient is in sandwichIngredients, let it be checked by default

// When user selects a bread, set breadChoice with the selected bread

// Render a "Next" button if breadChoice != {}