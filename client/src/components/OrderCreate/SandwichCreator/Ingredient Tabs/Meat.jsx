// Declare empty array var, meatChoice

// Render a list of meat ingredients using checkbox inputs
    // If the meat ingredient is in sandwichIngredients, let it be checked by default

// When user selects a meat, set meatChoice with the selected meat
// When a user deselects a meat, remove it meatChoice

// Render a "Next" button if meatChoice != []