// Declare empty array var, toppingChoice

// Render a list of topping ingredients using checkbox inputs
    // If the topping ingredient is in sandwichIngredients, let it be checked by default

// When user selects a topping, set toppingChoice with the selected topping
// When a user deselects a topping, remove it toppingChoice

// Render a "Complete Sandwich" button if toppingChoice != []